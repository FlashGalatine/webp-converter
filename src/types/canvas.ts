export interface CropBounds {
  x: number;
  y: number;
  width: number;
  height: number;
}

export interface CanvasPosition {
  x: number;
  y: number;
}

export interface ImageCoordinates {
  x: number;
  y: number;
}

